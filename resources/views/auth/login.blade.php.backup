@extends('layouts.auth.simple')

@section('content')
<div x-data="{ 
    darkMode: localStorage.getItem('theme') === 'dark',
    showPassword: false,
    init() {
        this.$watch('darkMode', val => localStorage.setItem('theme', val ? 'dark' : 'light'))
    }
}" 
:class="darkMode ? 'dark' : ''"
class="min-h-screen transition-all duration-500 flex items-center justify-center p-4 relative overflow-hidden">
    {{-- Background Image --}}
    <div class="absolute inset-0 -z-10"
        :style="darkMode 
            ? 'background-image: url(/images/darkmode backgorund.png); background-size: cover; background-position: center; background-attachment: fixed;' 
            : 'background-image: url(/images/lightmode background.png); background-size: cover; background-position: center; background-attachment: fixed;'">
    </div>
    
    {{-- Overlay --}}
    <div class="absolute inset-0 -z-10" :class="darkMode ? 'bg-black/40' : 'bg-black/20'"></div>

    {{-- Theme Toggle Button --}}
    <button @click="darkMode = !darkMode"
        class="fixed top-6 right-6 p-3 rounded-full backdrop-blur-md shadow-2xl hover:scale-110 transition-all duration-300 z-50 border"
        :class="darkMode 
            ? 'bg-zinc-900/80 border-zinc-700 text-yellow-400 hover:bg-zinc-800/90' 
            : 'bg-white/80 border-white/40 text-amber-600 hover:bg-white/90'">
        <svg x-show="darkMode" class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
        </svg>
        <svg x-show="!darkMode" class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
        </svg>
    </button>

    {{-- Main Container --}}
    <div class="w-full max-w-md backdrop-blur-xl rounded-3xl shadow-2xl overflow-hidden transition-all duration-500 border"
        :class="darkMode 
            ? 'bg-zinc-900/95 border-zinc-700/50' 
            : 'bg-white/98 border-white/50'">
        
        {{-- Header with Logo --}}
        <div class="px-8 pt-8 pb-6 text-center">
            <div class="flex justify-center mb-6">
                <img src="/images/stems-logo.png" alt="STEMS Logo" class="w-20 h-20 drop-shadow-lg">
            </div>
            <h1 class="text-3xl font-bold tracking-tight mb-2" :class="darkMode ? 'text-white' : 'text-gray-900'">
                Welcome Back
            </h1>
            <p class="text-sm" :class="darkMode ? 'text-gray-400' : 'text-gray-600'">
                Sign in to your STEMS account
            </p>
        </div>

        {{-- Form --}}
        <div class="px-8 pb-8">
            <form method="POST" action="{{ route('login') }}" class="space-y-6">
                @csrf
                
                {{-- Email Input --}}
                <div class="space-y-2">
                    <label for="email" class="block text-sm font-medium" :class="darkMode ? 'text-gray-200' : 'text-gray-700'">
                        Email Address
                    </label>
                    <input id="email" name="email" type="email" required autofocus
                        placeholder="Enter your email"
                        class="w-full px-4 py-3 rounded-xl border transition-all duration-300 placeholder-gray-400 text-sm focus:outline-none focus:ring-2 focus:ring-offset-0"
                        :class="darkMode
                            ? 'bg-zinc-800/50 border-zinc-600 text-white focus:ring-blue-500/50 focus:border-blue-500'
                            : 'bg-gray-50 border-gray-300 text-gray-900 focus:ring-blue-500/50 focus:border-blue-500'"
                        value="{{ old('email') }}" />
                    @error('email')
                        <p class="text-sm text-red-500 mt-1">{{ $message }}</p>
                    @enderror
                </div>

                {{-- Password Input --}}
                <div class="space-y-2">
                    <div class="flex justify-between items-center">
                        <label for="password" class="block text-sm font-medium" :class="darkMode ? 'text-gray-200' : 'text-gray-700'">
                            Password
                        </label>
                        @if (Route::has('password.request'))
                            <a href="{{ route('password.request') }}" class="text-sm font-medium transition-colors hover:underline"
                                :class="darkMode ? 'text-blue-400 hover:text-blue-300' : 'text-blue-600 hover:text-blue-700'">
                                Forgot password?
                            </a>
                        @endif
                    </div>
                    <div class="relative">
                        <input id="password" name="password" :type="showPassword ? 'text' : 'password'" required
                            placeholder="Enter your password"
                            class="w-full px-4 py-3 rounded-xl border transition-all duration-300 placeholder-gray-400 text-sm focus:outline-none focus:ring-2 focus:ring-offset-0 pr-12"
                            :class="darkMode
                                ? 'bg-zinc-800/50 border-zinc-600 text-white focus:ring-blue-500/50 focus:border-blue-500'
                                : 'bg-gray-50 border-gray-300 text-gray-900 focus:ring-blue-500/50 focus:border-blue-500'" />
                        <button type="button" @click="showPassword = !showPassword"
                            class="absolute right-3 top-1/2 -translate-y-1/2 p-1 rounded-md hover:bg-gray-100 dark:hover:bg-zinc-700 transition-colors"
                            :class="darkMode ? 'text-gray-400 hover:text-gray-300' : 'text-gray-500 hover:text-gray-700'">
                            <svg x-show="!showPassword" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                            </svg>
                            <svg x-show="showPassword" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.88 9.88l-3.29-3.29m7.532 7.532l3.29 3.29M3 3l3.59 3.59m0 0A9.953 9.953 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.025 10.025 0 01-4.132 5.411m0 0L21 21"></path>
                            </svg>
                        </button>
                    </div>
                    @error('password')
                        <p class="text-sm text-red-500 mt-1">{{ $message }}</p>
                    @enderror
                </div>

                {{-- Remember Me --}}
                <div class="flex items-center justify-between">
                    <label class="flex items-center space-x-2 cursor-pointer">
                        <input type="checkbox" name="remember" class="w-4 h-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500"
                            :class="darkMode ? 'accent-blue-500 bg-zinc-800 border-zinc-600' : 'accent-blue-600 bg-gray-50 border-gray-300'" />
                        <span class="text-sm" :class="darkMode ? 'text-gray-300' : 'text-gray-600'">Remember me</span>
                    </label>
                </div>

                {{-- Sign In Button --}}
                <button type="submit" class="w-full py-3.5 rounded-xl font-semibold text-white transition-all duration-300 hover:shadow-lg hover:scale-[1.02] active:scale-95"
                    :class="darkMode
                        ? 'bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-700 hover:to-blue-600 shadow-lg shadow-blue-500/30'
                        : 'bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-700 hover:to-blue-600 shadow-lg shadow-blue-500/30'">
                    Sign In
                </button>
            </form>

            {{-- Divider --}}
            <div class="relative my-8">
                <div class="absolute inset-0 flex items-center">
                    <div class="w-full border-t" :class="darkMode ? 'border-zinc-600' : 'border-gray-300'"></div>
                </div>
                <div class="relative flex justify-center text-sm">
                    <span :class="darkMode ? 'bg-zinc-900 px-4 text-gray-400' : 'bg-white px-4 text-gray-500'">Or continue with</span>
                </div>
            </div>

            {{-- Social Buttons --}}
            <div class="grid grid-cols-3 gap-3">
                <button type="button" class="p-3 rounded-xl border transition-all duration-300 hover:scale-105 shadow-sm hover:shadow-md"
                    :class="darkMode ? 'bg-zinc-800/50 border-zinc-600 hover:bg-zinc-700/50' : 'bg-gray-50 border-gray-300 hover:bg-gray-100'">
                    <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/google/google-original.svg" alt="Google" class="w-5 h-5 mx-auto">
                </button>
                <button type="button" class="p-3 rounded-xl border transition-all duration-300 hover:scale-105 shadow-sm hover:shadow-md"
                    :class="darkMode ? 'bg-zinc-800/50 border-zinc-600 hover:bg-zinc-700/50' : 'bg-gray-50 border-gray-300 hover:bg-gray-100'">
                    <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/apple/apple-original.svg" alt="Apple" class="w-5 h-5 mx-auto">
                </button>
                <button type="button" class="p-3 rounded-xl border transition-all duration-300 hover:scale-105 shadow-sm hover:shadow-md"
                    :class="darkMode ? 'bg-zinc-800/50 border-zinc-600 hover:bg-zinc-700/50' : 'bg-gray-50 border-gray-300 hover:bg-gray-100'">
                    <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/facebook/facebook-original.svg" alt="Facebook" class="w-5 h-5 mx-auto">
                </button>
            </div>

            {{-- Sign Up Link --}}
            <p class="text-center text-sm mt-8" :class="darkMode ? 'text-gray-400' : 'text-gray-600'">
                Don't have an account?
                @if (Route::has('register'))
                    <a href="{{ route('register') }}" class="font-semibold transition-colors hover:underline ml-1"
                        :class="darkMode ? 'text-blue-400 hover:text-blue-300' : 'text-blue-600 hover:text-blue-700'">
                        Sign up
                    </a>
                @endif
            </p>
        </div>
    </div>
</div>
@endsection
                                    <svg x-show="!showPassword" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                                    </svg>
                                    <svg x-show="showPassword" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.88 9.88l-3.29-3.29m7.532 7.532l3.29 3.29M3 3l3.59 3.59m0 0A9.953 9.953 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.025 10.025 0 01-4.132 5.411m0 0L21 21"></path>
                                    </svg>
                                </button>
                            </div>
                            @error('password')
                                <p class="text-sm text-red-500">{{ $message }}</p>
                            @enderror
                        </div>

                        {{-- Remember Me --}}
                        <label class="flex items-center space-x-2 cursor-pointer">
                            <input type="checkbox" name="remember" class="w-4 h-4 rounded"
                                :class="darkMode ? 'accent-blue-500 bg-zinc-800 border-zinc-700' : 'accent-blue-600 bg-gray-50 border-gray-300'" />
                            <span class="text-sm" :class="darkMode ? 'text-gray-300' : 'text-gray-600'">Remember me</span>
                        </label>

                        {{-- Sign In Button --}}
                        <button type="submit" class="w-full py-3 rounded-lg font-semibold text-white transition-all duration-300 hover:shadow-lg hover:scale-[1.02] active:scale-95 mt-8"
                            :class="darkMode
                                ? 'bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-700 hover:to-blue-600 shadow-lg shadow-blue-500/30'
                                : 'bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-700 hover:to-blue-600 shadow-lg shadow-blue-500/30'">
                            Sign in
                        </button>
                    </form>

                    {{-- Divider --}}
                    <div class="relative">
                        <div class="absolute inset-0 flex items-center">
                            <div class="w-full border-t" :class="darkMode ? 'border-zinc-700' : 'border-gray-300'"></div>
                        </div>
                        <div class="relative flex justify-center text-sm">
                            <span :class="darkMode ? 'bg-zinc-900 text-gray-400' : 'bg-white text-gray-500'">Or continue with</span>
                        </div>
                    </div>

                    {{-- Social Buttons --}}
                    <div class="grid grid-cols-3 gap-3">
                        <button type="button" class="p-3 rounded-lg border transition-all duration-300 hover:scale-110 shadow-sm hover:shadow-md"
                            :class="darkMode ? 'bg-zinc-800 border-zinc-700' : 'bg-gray-50 border-gray-300'">
                            <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/google/google-original.svg" alt="Google" class="w-5 h-5 mx-auto">
                        </button>
                        <button type="button" class="p-3 rounded-lg border transition-all duration-300 hover:scale-110 shadow-sm hover:shadow-md"
                            :class="darkMode ? 'bg-zinc-800 border-zinc-700' : 'bg-gray-50 border-gray-300'">
                            <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/apple/apple-original.svg" alt="Apple" class="w-5 h-5 mx-auto">
                        </button>
                        <button type="button" class="p-3 rounded-lg border transition-all duration-300 hover:scale-110 shadow-sm hover:shadow-md"
                            :class="darkMode ? 'bg-zinc-800 border-zinc-700' : 'bg-gray-50 border-gray-300'">
                            <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/facebook/facebook-original.svg" alt="Facebook" class="w-5 h-5 mx-auto">
                        </button>
                    </div>

                    {{-- Sign Up Link --}}
                    <p class="text-center text-sm" :class="darkMode ? 'text-gray-300' : 'text-gray-600'">
                        Don't have an account?
                        @if (Route::has('register'))
                            <a href="{{ route('register') }}" class="font-semibold transition-colors hover:underline"
                                :class="darkMode ? 'text-blue-400 hover:text-blue-300' : 'text-blue-600 hover:text-blue-700'">
                                Sign up
                            </a>
                        @endif
                    </p>
                </div>
            </div>

            {{-- RIGHT: Branding Section --}}
            <div class="hidden md:flex flex-1 items-center justify-center p-12 transition-all duration-500 relative overflow-hidden"
                :class="darkMode 
                    ? 'bg-gradient-to-br from-zinc-800/50 to-zinc-900/50' 
                    : 'bg-gradient-to-br from-blue-50/50 to-indigo-50/50'">
                
                {{-- Gradient Blobs --}}
                <div class="absolute top-0 right-0 w-96 h-96 bg-gradient-to-br from-blue-500/10 to-purple-500/10 rounded-full -translate-y-1/2 translate-x-1/2 blur-3xl"></div>
                <div class="absolute bottom-0 left-0 w-96 h-96 bg-gradient-to-tr from-blue-500/10 to-cyan-500/10 rounded-full translate-y-1/2 -translate-x-1/2 blur-3xl"></div>

                <div class="text-center space-y-8 relative z-10">
                    {{-- Logo --}}
                    <div class="flex justify-center">
                        <img src="/images/stems-logo.png" alt="STEMS Logo" class="w-40 h-40 drop-shadow-2xl hover:scale-105 transition-transform duration-300">
                    </div>

                    {{-- Branding Text --}}
                    <div class="space-y-4">
                        <h1 class="text-5xl font-bold tracking-tight" :class="darkMode ? 'text-white' : 'text-gray-900'">
                            STEMS
                        </h1>
                        <p class="text-lg max-w-xs mx-auto leading-relaxed" :class="darkMode ? 'text-gray-300' : 'text-gray-700'">
                            Swiper Tools & Equipment Management System
                        </p>
                        <p class="text-sm max-w-sm mx-auto" :class="darkMode ? 'text-gray-400' : 'text-gray-600'">
                            Streamline your equipment rental operations with our modern and intuitive platform
                        </p>
                    </div>

                    {{-- Feature Badges --}}
                    <div class="flex flex-wrap gap-3 justify-center pt-4">
                        <div class="px-4 py-2 rounded-full backdrop-blur-sm" :class="darkMode ? 'bg-white/10 border border-white/20' : 'bg-white/40 border border-white/60'">
                            <span class="text-xs font-semibold" :class="darkMode ? 'text-gray-200' : 'text-gray-700'">🚀 Easy to Use</span>
                        </div>
                        <div class="px-4 py-2 rounded-full backdrop-blur-sm" :class="darkMode ? 'bg-white/10 border border-white/20' : 'bg-white/40 border border-white/60'">
                            <span class="text-xs font-semibold" :class="darkMode ? 'text-gray-200' : 'text-gray-700'">⚡ Fast</span>
                        </div>
                        <div class="px-4 py-2 rounded-full backdrop-blur-sm" :class="darkMode ? 'bg-white/10 border border-white/20' : 'bg-white/40 border border-white/60'">
                            <span class="text-xs font-semibold" :class="darkMode ? 'text-gray-200' : 'text-gray-700'">🔒 Secure</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection